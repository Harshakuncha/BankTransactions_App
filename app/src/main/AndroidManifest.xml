<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mybankproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
       android:theme="@style/Theme.Mybankproject">


        <activity android:name=".Activities.SendtoUserList"></activity>
        <activity android:name=".Activities.UserData" />
        <activity android:name=".Activities.TransactionHistory" />
        <activity android:name=".Activities.users_list" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>